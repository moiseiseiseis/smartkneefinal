version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: sevid
      POSTGRES_PASSWORD: sevid
      POSTGRES_DB: sevid_dev
    ports:
      - "5432:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data
volumes:
  dbdata:
